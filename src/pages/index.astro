---
const title = 'Joba Tea'
const description = 'Job search tracker with clear next steps.'
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content={description} />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<style>
			@import url("https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600;9..144,700&family=Work+Sans:wght@400;500;600;700&display=swap");

			:root {
				--bg: #fbf4e8;
				--bg2: #fdf8f1;
				--text: rgba(17, 24, 39, 0.92);
				--muted: rgba(17, 24, 39, 0.7);
				--line: rgba(17, 24, 39, 0.12);
				--pink: #f6c6e8;
				--blue: #bfe6ff;
			}

			* {
				box-sizing: border-box;
			}

			body {
				margin: 0;
				font-family: "Work Sans", ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica,
					Arial, "Apple Color Emoji", "Segoe UI Emoji";
				color: var(--text);
				min-height: 100dvh;
				display: flex;
				align-items: center;
				background:
					radial-gradient(900px 520px at 10% 0%, rgba(246, 198, 232, 0.35), transparent 60%),
					radial-gradient(900px 520px at 90% 0%, rgba(191, 230, 255, 0.4), transparent 60%),
					linear-gradient(180deg, var(--bg2), var(--bg));
				background-repeat: no-repeat;
				background-size: 100% 100%;
			}

			a {
				color: inherit;
				text-decoration: none;
			}

			.container {
				max-width: 720px;
				margin: 0 auto;
				padding: 28px 20px 72px;
				width: 100%;
			}

			header {
				display: flex;
				align-items: center;
				justify-content: space-between;
				gap: 12px;
				padding: 8px 0 36px;
			}

			.brand {
				display: flex;
				align-items: center;
				gap: 10px;
				font-weight: 700;
			}

			.logo {
				width: 28px;
				height: 28px;
				border-radius: 10px;
				background: linear-gradient(135deg, var(--pink), var(--blue));
				border: 1px solid rgba(17, 24, 39, 0.14);
			}

			.badge {
				display: inline-flex;
				align-items: center;
				gap: 8px;
				padding: 6px 10px;
				border-radius: 999px;
				border: 1px solid var(--line);
				color: var(--muted);
				font-size: 12px;
			}

			.stack {
				display: grid;
				gap: 14px;
			}

			.hero {
				display: grid;
				gap: 12px;
				margin-bottom: 6px;
			}

			h1 {
				margin: 0;
				font-family: "Fraunces", "Times New Roman", serif;
				font-size: clamp(2.8rem, 5vw, 4.2rem);
				letter-spacing: -0.02em;
				line-height: 1.04;
			}

			.highlight {
				background: linear-gradient(120deg, rgba(246, 198, 232, 0.9), rgba(191, 230, 255, 0.9));
				padding: 0 10px 2px;
				border-radius: 999px;
				box-decoration-break: clone;
			}

			.description {
				margin: 0;
				font-size: 1.05rem;
				line-height: 1.6;
				color: var(--muted);
				max-width: 560px;
			}

			.actions {
				display: flex;
				gap: 10px;
				flex-wrap: wrap;
			}

			.btn {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				padding: 12px 14px;
				border-radius: 14px;
				border: 1px solid rgba(17, 24, 39, 0.14);
				background: rgba(255, 255, 255, 0.8);
				font-weight: 650;
			}

			.btn.primary {
				background: linear-gradient(135deg, rgba(246, 198, 232, 0.95), rgba(191, 230, 255, 0.95));
			}

			.meta {
				color: var(--muted);
				font-size: 13px;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<header>
				<div class="brand">
					<div class="logo" aria-hidden="true"></div>
					<div>Joba Tea</div>
				</div>
				<div class="badge">No auto apply. No outreach.</div>
			</header>

			<main class="stack">
				<div class="hero">
					<h1>
						Do the hard parts of applying with
						<span class="highlight">Joba Tea</span>.
					</h1>
					<p class="description">
						Organize every email, call, and follow‑up in one calm timeline. Get clear
						recommendations on what to do next, plus study ideas tailored to each role so
						you can move fast without losing track.
					</p>
				</div>
				<!--
				<div class="actions">
					<a
						class="btn primary"
						href="mailto:hello@jobatea.com?subject=Joba%20Tea%20trial&body=I%20want%20to%20start%20a%20trial.%0A%0ATrack%3A%20%0A"
					>
						Start free trial
					</a>
					<a class="btn" href="/more">More</a>
				</div>
				<div class="meta">$5/month after trial</div>
				-->
			</main>
		</div>
	</body>
</html>
