---
const title = 'Joba Tea'
const description =
  'Know what to do next in your job search. Interviews, follow-ups, and prep tasks in one calm, clear system.'
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content={description} />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<style>
			:root {
				/* Light tan base */
				--bg: #fbf4e8;
				--bg2: #fdf8f1;

				/* Surfaces */
				--card: rgba(255, 255, 255, 0.78);
				--card2: rgba(255, 255, 255, 0.92);

				/* Text */
				--text: rgba(17, 24, 39, 0.92);
				--muted: rgba(17, 24, 39, 0.72);
				--faint: rgba(17, 24, 39, 0.52);
				--line: rgba(17, 24, 39, 0.12);

				/* Soft pastel accents */
				--pink: #f6c6e8;
				--blue: #bfe6ff;
				--mint: #c8f5e3;
				--lav: #e6dbff;
				--peach: #ffd7c2;
				--sand: #f3e2c9;

				--shadow: 0 16px 44px rgba(56, 44, 22, 0.12);
				--ink: #121826;
			}

			* {
				box-sizing: border-box;
			}

			body {
				margin: 0;
				font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial,
					"Apple Color Emoji", "Segoe UI Emoji";
				color: var(--text);
				background:
					radial-gradient(1100px 700px at 10% 0%, rgba(243, 226, 201, 0.9), transparent 60%),
					radial-gradient(900px 520px at 18% 10%, rgba(246, 198, 232, 0.45), transparent 55%),
					radial-gradient(900px 520px at 85% 0%, rgba(191, 230, 255, 0.45), transparent 55%),
					radial-gradient(900px 620px at 70% 95%, rgba(200, 245, 227, 0.4), transparent 60%),
					linear-gradient(180deg, var(--bg2), var(--bg));
				background-repeat: no-repeat;
				background-size: 100% 100%;
			}

			a {
				color: inherit;
				text-decoration: none;
			}
			a:hover {
				text-decoration: underline;
			}

			.container {
				max-width: 980px;
				margin: 0 auto;
				padding: 28px 20px 84px;
			}

			header {
				display: flex;
				align-items: center;
				justify-content: space-between;
				gap: 14px;
				padding: 8px 0 30px;
			}

			.brand {
				display: flex;
				align-items: center;
				gap: 10px;
				font-weight: 700;
				letter-spacing: 0.2px;
				color: rgba(18, 24, 38, 0.92);
			}

			.logo {
				width: 28px;
				height: 28px;
				border-radius: 10px;
				background: linear-gradient(135deg, var(--pink), var(--blue));
				border: 1px solid rgba(17, 24, 39, 0.14);
				box-shadow: 0 10px 26px rgba(18, 24, 38, 0.12);
			}

			.badge {
				display: inline-flex;
				align-items: center;
				gap: 8px;
				padding: 8px 12px;
				border-radius: 999px;
				background: rgba(255, 255, 255, 0.7);
				border: 1px solid rgba(17, 24, 39, 0.12);
				color: var(--muted);
				font-size: 13px;
			}

			.hero {
				display: grid;
				grid-template-columns: 1.25fr 0.75fr;
				gap: 18px;
				align-items: start;
				margin-top: 4px;
			}

			@media (max-width: 900px) {
				.hero {
					grid-template-columns: 1fr;
				}
			}

			h1 {
				font-size: clamp(32px, 4vw, 48px);
				line-height: 1.08;
				margin: 10px 0 12px;
				letter-spacing: -0.02em;
				color: rgba(18, 24, 38, 0.94);
			}

			.subhead {
				font-size: 16px;
				line-height: 1.55;
				color: var(--muted);
				max-width: 66ch;
				margin: 0 0 18px;
			}

			.ctaRow {
				display: flex;
				gap: 10px;
				flex-wrap: wrap;
				margin-top: 14px;
			}

			.btn {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				padding: 12px 14px;
				border-radius: 14px;
				border: 1px solid rgba(17, 24, 39, 0.14);
				background: rgba(255, 255, 255, 0.76);
				color: rgba(18, 24, 38, 0.92);
				font-weight: 650;
				letter-spacing: 0.1px;
				box-shadow: 0 10px 22px rgba(18, 24, 38, 0.08);
			}

			.btn.primary {
				background: linear-gradient(135deg, rgba(246, 198, 232, 0.95), rgba(191, 230, 255, 0.95));
			}

			.btn:hover {
				transform: translateY(-1px);
			}

			.card {
				background: linear-gradient(180deg, var(--card2), var(--card));
				border: 1px solid rgba(17, 24, 39, 0.12);
				border-radius: 18px;
				padding: 16px;
				box-shadow: var(--shadow);
			}

			.card h3 {
				margin: 0 0 8px;
				font-size: 15px;
				letter-spacing: 0.1px;
				color: rgba(18, 24, 38, 0.92);
			}

			.card p {
				margin: 0;
				color: var(--muted);
				font-size: 14px;
				line-height: 1.55;
			}

			.grid {
				display: grid;
				grid-template-columns: repeat(2, 1fr);
				gap: 12px;
				margin-top: 16px;
			}

			@media (max-width: 900px) {
				.grid {
					grid-template-columns: 1fr;
				}
			}

			.section {
				margin-top: 34px;
				padding-top: 22px;
				border-top: 1px solid var(--line);
			}

			.kicker {
				font-size: 12px;
				color: var(--faint);
				letter-spacing: 0.14em;
				text-transform: uppercase;
				margin: 0 0 10px;
			}

			.h2 {
				font-size: 20px;
				margin: 0 0 10px;
				color: rgba(18, 24, 38, 0.92);
			}

			.list {
				margin: 10px 0 0;
				padding-left: 18px;
				color: var(--muted);
				line-height: 1.7;
			}

			.split {
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 12px;
				margin-top: 14px;
			}

			@media (max-width: 900px) {
				.split {
					grid-template-columns: 1fr;
				}
			}

			.pricing {
				display: flex;
				align-items: baseline;
				gap: 10px;
				flex-wrap: wrap;
				margin-top: 12px;
			}

			.price {
				font-size: 22px;
				font-weight: 750;
			}

			.mini {
				color: var(--faint);
				font-size: 13px;
			}

			footer {
				margin-top: 44px;
				color: var(--faint);
				font-size: 13px;
				line-height: 1.7;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<header>
				<div class="brand">
					<div class="logo" aria-hidden="true"></div>
					<div>Joba Tea</div>
				</div>
				<div class="badge">No auto apply. No outreach on your behalf.</div>
			</header>

			<section class="hero">
				<div>
					<p class="kicker">Job search command center</p>
					<h1>Always know your next move.</h1>
					<p class="subhead">
						Joba Tea keeps interviews, follow-ups, and prep tasks in one place so you stay organized without juggling
						tools.
					</p>
					<p class="subhead" style="margin-top: -6px;">Clear plan. Fewer tabs. Better follow-through.</p>

					<div class="ctaRow">
						<a class="btn primary" href="#start">Start free</a>
						<a class="btn" href="#features">See features</a>
					</div>

					<div class="pricing" aria-label="Pricing summary">
						<div class="price">$5/month</div>
						<div class="mini">after trial</div>
					</div>
				</div>

				<div class="card">
					<h3>Core idea</h3>
					<p>
						One clear view of what matters right now. Your dashboard answers one question each day: what should I do
						next?
					</p>
					<ul class="list">
						<li>Upcoming interviews</li>
						<li>Overdue actions</li>
						<li>Stalled conversations</li>
						<li>Prep tasks</li>
					</ul>
				</div>
			</section>

			<section class="section" id="problem">
				<p class="kicker">Problem</p>
				<h2 class="h2">Job searching gets fragmented fast.</h2>
				<p class="subhead">
					Notes live in docs. Dates live in calendars. Prep lives in your head. Follow-ups slip because nothing shows
					you what needs attention today. That friction adds stress and costs opportunities.
				</p>
				<p class="subhead" style="margin-top: -6px;">Joba Tea pulls it together.</p>
			</section>

			<section class="section" id="features">
				<p class="kicker">Features</p>
				<h2 class="h2">A system you can rely on.</h2>

				<div class="grid">
					<div class="card">
						<h3>Stay on top of interviews</h3>
						<p>See upcoming interviews by day with time, format, and role clearly shown.</p>
					</div>
					<div class="card">
						<h3>Remember every conversation</h3>
						<p>
							Each job has a clean timeline for interviews, emails, notes, contacts, and links. Open a job and regain
							context fast.
						</p>
					</div>
					<div class="card">
						<h3>Prepare with intent</h3>
						<p>Your practice queue reflects real interviews, not random grinding.</p>
					</div>
					<div class="card">
						<h3>Track momentum without pressure</h3>
						<p>
							Activity counts show effort and progress without turning your search into a performance dashboard.
						</p>
					</div>
				</div>

				<div class="split" style="margin-top: 14px;">
					<div class="card">
						<h3>Details stay attached to the job</h3>
						<ul class="list">
							<li>Original posting link</li>
							<li>Salary range and equity fields when available</li>
							<li>Markdown job description</li>
							<li>Auto tags parsed from the description</li>
						</ul>
					</div>
					<div class="card">
						<h3>Company context</h3>
						<ul class="list">
							<li>All contacts at a company with email and phone</li>
							<li>Last touch per contact</li>
							<li>Links and notes that do not get lost</li>
						</ul>
					</div>
				</div>
			</section>

			<section class="section" id="not">
				<p class="kicker">What this is not</p>
				<h2 class="h2">A calm tool that stays in its lane.</h2>
				<ul class="list">
					<li>No auto-apply.</li>
					<li>No outreach on your behalf.</li>
					<li>No forced workflows.</li>
					<li>Just a clear, organized view while you do the work that matters.</li>
				</ul>
			</section>

			<section class="section" id="pricing">
				<p class="kicker">Pricing</p>
				<h2 class="h2">Simple pricing, no surprises.</h2>
				<div class="card">
					<div class="pricing">
						<div class="price">$5 per month</div>
						<div class="mini">Unlimited jobs, interviews, timelines, and prep. Cancel anytime.</div>
					</div>
				</div>
			</section>

			<section class="section" id="start">
				<p class="kicker">Close</p>
				<h2 class="h2">Stay organized. Show up ready.</h2>
				<p class="subhead">
					Job searching is demanding enough. Let Joba Tea handle the tracking so you can focus on thinking clearly and
					preparing well.
				</p>
				<div class="ctaRow">
					<a
						class="btn primary"
						href="mailto:hello@jobatea.com?subject=Joba%20Tea%20trial&body=I%20want%20to%20start%20a%20trial.%0A%0ATrack%3A%20%0A"
					>
						Start your free trial
					</a>
					<a class="btn" href="#pricing">View pricing</a>
				</div>
			</section>

			<footer>
				<div class="badge">No auto apply. No outreach on your behalf. Clear next steps.</div>
				<div style="margin-top: 10px;">Lightweight by design. Built for steady execution.</div>
			</footer>
		</div>
	</body>
</html>
